---
scope: predictgame7 whole-system spine: ratify existing codebase conventions + fix PRD-deferred decisions (NFR-V1 analytics isolation, FR-20/21 pipeline incl. series.status enumeration, FR-31 share-link/OG-card)
purpose: build substrate for agent-driven implementation (bmad-build, epics, future sessions)
altitude: initiative
updated: 2026-09-23T08:50
---

- (constraint) PRD binds: NFR-V1 analytics isolation; NFR-S1 secrets/RLS/write-boundary; free-tier cost ceiling (Supabase/GH Pages/PostHog/email); GH Pages static SPA hosting; NBA-only; no live scoring (pipeline cadence only)
- (constraint) Owner-settled (inherit, do not re-decide): pipeline script-based + CI-scheduled per supabase/scripts precedent; Q-4 Fantrax API preferred / nba.com scrape fallback, feasibility spike is build prerequisite; sharing = deep-links + OG cards (FR-31 option A); PostHog deprecatable
- (event) Code sweep done. Divergences found: PostHog inline in 4 files + direct posthog-js import in dead AuthContext vs usePostHog hook in pages; PredictionInput/Result types duplicated and drifted (frontend allows 'bayesian'/'ensemble_v1'/'margin_model_v1', function knows 'bayes'+4 slugs; PredictionResult fields function never returns); two Deno styles (predict-game-7: Deno.serve+jsr vs handle-contact: deno.land std@0.168 serve+esm.sh); two type files (types.ts + index.ts); @ alias configured but some relative imports; RHF+zod installed but unused (contact form manual); AuthProvider never rendered (vestigial); no prerender/OG today (PageMeta exists, used only by unrouted pages; index.html bare meta); deep-link precedent: ?series=<id> in PredictPage
- (version) Stack (package.json): react ^18, vite=npm:rolldown-vite@latest, @supabase/supabase-js 2.103.1 pinned, posthog-js ^1.376.4, @posthog/react ^1.9.1, react-router-dom ^7.9.5, tailwindcss ^3.4.11, zod ^3.25.76, react-hook-form ^7.66.0, @biomejs/biome 2.4.5, react-helmet-async present; edge runtime Deno (jsr:@supabase/supabase-js@2); legacy pipeline Python+pandas
- (decision) Canonical Method slugs = logistic_regression | bayes | elo | exponential_smoothing (edge function is source of truth today; frontend union drifted) - binds AD-2, prevents method-select bugs like issue #3
- (decision) AD-1 analytics isolation: single module src/lib/analytics (typed event registry, track/identify/reset/captureException); posthog-js imported nowhere else; 10 event names preserved verbatim (addendum A.1) [ASSUMPTION shape pending owner review]
- (decision) AD-3 edge function convention: Deno.serve + jsr imports + shared _shared/ helpers (CORS, jsonResponse, contract types); handle-contact migrated off legacy deno.land std@0.168 style
- (decision) AD-4 series.status enumeration proposal: scheduled|active|completed (text + CHECK); Active Series = status='active'; Historical Archive = 'completed'; migration + CURRENT_DATA_MODEL update required [ASSUMPTION pending owner review]
- (decision) AD-5 pipeline: GH Actions scheduled workflow (offseason/inseason modes) running scripts under supabase/scripts/pipeline with service_role from GH secrets; data source behind SeriesDataSource port (fantrax/nba.com/manual-csv adapters, env-selected); idempotent upsert keyed (year,round); loud failure [ASSUMPTION adapter shape pending spike]
- (decision) AD-6 sharing: Share Link = site deep-link with encoded prediction ref; OG image generated at runtime by new Supabase Edge Function (satori+resvg - Supabase official guide, web-verified); per-link crawler meta via edge-rendered share landing that redirects humans to the site deep-link [ASSUMPTION - main fork, owner review flagged]
- (decision) AD-7 SEO: build-time prerender for the 177 historical series pages (known at build; static HTML per route at deploy); runtime edge route only for share landings [ASSUMPTION]
- (decision) AD-8 data boundary: reads = client-side supabase-js direct from pages (ratified, single client src/db/supabase.ts); ALL writes/mutations = Edge Functions only with service_role (NFR-S1); no service key ever in client
- (event) Web verification: Supabase official OG-image Edge Function guide exists (satori+resvg on Deno) - AD-6 mechanism viable on free tier; Fantrax API current status NOT confirmable via search - Q-4 spike stays a hard prerequisite; nba.com stats endpoints reachable via nba_api-style clients but rate-limit/Cloudflare risk documented
- (decision) Owner review 2026-09-23: AD-1 confirmed (single PostHog location). AD-4 changed to TWO values active|completed (prior 3-value attempt caused issues); series rows exist only when active; backfill existing->completed. AD-6 adopted under zero-extra-cost constraint: share link = share-og Edge Function URL serving per-link meta + generated card, redirect to site deep-link with utm_source=share; prerender SaaS/Vercel rejected. AD-7 confirmed; prerender content = series facts (scores, winner, meta, CTA) NOT predictions. AD-5 confirmed. Staging deferred with explicit revisit trigger = before FR-22 accounts build (2nd Supabase project + branch deploy; moderate setup, migration-sync upkeep)
- (event) Reconciliation pass: lint clean; 2 med + 5 low gaps fixed in spine - AD-1 extended with provider-agnostic metric-query surface (NFR-V1 covers FR-25 queries) + SM-1 UV-definition pinning duty + agent-skill-folder exit; AD-3 gained Resend server-side-only rule (key never VITE_*, free tier); AD-5 failure alerting defined as GH failed-run notification (SM-4); NFR-P1 performance convention row added; Resend stack row; test framework deferred to FR-30 build (Vitest candidate, verify rolldown-vite compat)
- (event) Reviewer gate ran (rubric walker, web verification, adversarial) — reviews in {ws}/reviews/. Findings triaged; clear fixes applied to spine.
- (decision) AD-6/AD-7 amended: GH Pages 404s all deep-links (verified live), so build chain emits dist/404.html SPA fallback and archive share links target the prerendered /series/<id> route (dist/series/<id>/index.html). Prevents: share links dead as deployed.
- (decision) AD-2 amended: contract owns SharePayload schema (url-safe base64) so Share button and share-og cannot diverge; probability scale pinned to 0-100 percent (what the function actually returns) — resolves conventions-table contradiction.
- (decision) AD-4 amended: live DB has three-value chk_series_status + DEFAULT 'historical' (migration 00005) and HistoricalPage queries 'historical' — migration must replace constraint, backfill historical->completed, and ship in same release as frontend query flip, scheduled outside playoff window. Prevents: silently emptying the archive.
- (decision) AD-5 amended: (year,round) upsert key requires prerequisite migration adding UNIQUE(year,round) + CHECK domain on round (today free TEXT); pipeline owns insights_cache refresh (FR-12).
- (decision) AD-6 amended: share-og is anonymous/public (no JWT — crawlers must get 200); card generation re-pointed to npm:@vercel/og (Supabase's current official pattern); <1s redirect unverified vs Edge CPU limits — measure in FR-31 spike.
- (decision) AD-1 amended: FR-25 metric-query surface documented/typed in src/lib/analytics but executes owner-local (personal PostHog API key never in repo/bundle/VITE_*).
- (version) npm:rolldown-vite@latest is a deprecated Vite-7 shim (7.3.1); Vite 8 is Rolldown-powered — Stack directs migration to vite@^8 at next dependency touch. Vitest ^4.1 supports Vite 8 (resolves Deferred test-framework question). Tailwind stays v3.4 deliberately (v4 upgrade = standalone task). React Router v8 exists (ESM-only); ^7.9.5 stands.
- (constraint) Deployment mechanics pinned: Edge Functions via supabase CLI deploy; function secrets via supabase secrets/dashboard, never repo or VITE_*; migrations apply before matching gh-pages deploy.
- (event) spine finalized
