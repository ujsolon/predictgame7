# Reconcile: SPEC.md vs UX spines (scoped run — 4 new surfaces)

Checked `_bmad-output/specs/spec-predictgame7/SPEC.md` against `DESIGN.md` and `EXPERIENCE.md` in this folder. Scope: OG share card, `/series/<id>`, Share button + success states, error/empty states only. Existing surfaces and parked items (social-temperature widget, similar-series slot) not flagged.

- **CAP-8 / AD-7 — "game-by-game scores" on series page.** SPEC: static HTML per `/series/<id>` must contain "real meta, game-by-game scores, and a Predict CTA". DESIGN.md's series-page hero specs the score strip as "final series score + Game 7 box" only; neither spine shows a per-game listing for the whole series. Fix: add a game-by-game score row (every game, `mono-data tabular-nums`, hairline cells) to the series-page layout and Components spec.

- **CAP-8 / AD-7 — full content for a JS-disabled crawler.** SPEC: "initial HTML carries full content to a JS-disabled crawler; active/custom routes stay client-rendered." EXPERIENCE.md Foundation frames the product as a plain SPA and neither spine mentions static pre-rendering of the series route. Fix: state in Foundation/IA that `/series/<id>` is build-emitted static HTML (content + meta present before JS), with client-rendered behavior layered on; only active/custom routes stay client-rendered.

- **CAP-7 / FR-31 — `og:title` and `og:description` content.** SPEC success: "`og:title/description/image` render on major social surfaces." The spines spec only the image (three OG card variants); no title/description copy is defined for historic, custom-matchup, or fallback URLs, and CAP-8's "real meta" per series page is likewise unaddressed. Fix: add meta title/description copy patterns per variant (e.g. "Cavs vs Warriors — Game 7, 2016 Finals" + one voice-consistent line) to EXPERIENCE.md Component Patterns / Voice and Tone.

- **CAP-6 / AD-6 — UTM convention on shared links.** SPEC: "share-link arrivals are attributable via the UTM convention (AD-6)" and CAP-7: "arrivals count toward SM-3." EXPERIENCE.md lists shareable URL shapes (`/series/<id>?method=<slug>`, `/predict?custom=<base64>`) with no UTM parameters, and the Share button behavior never mentions them. Fix: require the Share button's copied/native-share URL to carry the AD-6 UTM parameters; note it in Component Patterns → Share button.

- **CAP-7 / FR-31 — arrival behavior of the historic share link.** SPEC: "opening a Share Link re-renders the prediction with zero re-entry." Flow 1 covers the custom link round-trip, but the spine never says what `/series/<id>?method=<slug>` renders on arrival — the `method` param is listed and then dropped. (AD-7 only bans prediction outputs *baked into* the static HTML; a client-rendered re-render from the query param is compatible.) Fix: specify in State Patterns/Key Flows what a `?method=<slug>` arrival shows (e.g. Predict CTA pre-seeded with that method, or an in-page result panel) so FR-31's zero-re-entry holds for historic shares too.

- **Constraints / Non-goals — video FR-13 is PLANNED-GATED.** SPEC: "PLANNED-GATED features (... video FR-13) must not be built without an explicit owner decision" and "Series video content (FR-13) — not scheduled before the gate." Both spines nonetheless spec a video embed + credit component and a video-facade interaction as a standard flagship-page section (DESIGN.md Components; EXPERIENCE.md Component Patterns, Interaction Primitives, Accessibility Floor, Flow 2). This contradicts the gate. Fix: either remove the video component or explicitly mark the slot as gated/conditional on an owner decision, mirroring how the similar-series slot is marked optional — and drop the video facade gesture/accessibility rules to that conditional slot.

No conflicts found on: error-state taxonomy (invalid input vs service failure, FR-8), retry safety + input preservation, 404 SPA-fallback treatment for unknown series ids, "no prediction outputs baked in" (probability-bar rejection), never-post-on-user's-behalf (native sheet/clipboard only, no platform menus), analytics isolation NFR-V1 (tracker-leak rejection), WCAG 2.1 AA floor, responsive behavior, no-live-scoring, and empty-Active-list/empty-archive copy.
